from UsingWebSocket.ws_client import start_ws

if __name__ == "__main__":
    print("Starting WebSocket client...")
    start_ws()
    # ani = FuncAnimation(fig, animate, interval=60000)  # update mỗi 60 giây
    # plt.show()